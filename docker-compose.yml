version: '3'
services:
  redis_db:
    environment:
        REDIS_ARGS: --requirepass password
    image: redis/redis-stack-server:latest
    volumes:
      - redis-data:/etc/data
    ports:
      - 6379:6379
volumes:
  redis-data:
